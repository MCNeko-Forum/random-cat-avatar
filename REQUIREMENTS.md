# 随机 SVG 头像生成器 — 需求文档

## 1. 项目概述

纯前端单页应用：用户输入任意字符串作为 seed，通过 SHA-256 哈希驱动确定性随机数生成器（PRNG），组装出唯一的 SVG **猫咪**头像（卡通线条风格：描边 + 圆润曲线，贴纸感）。**同一 seed 永远生成同一头像**（刷新、换设备、换浏览器结果一致）。

- 技术栈：HTML、CSS、JavaScript（原生 `<script>` 标签顺序加载，不用 ES Module——保证 `file://` 双击直开也能跑）、SVG、Web Crypto API
- 纯静态、零依赖、无构建步骤、无后端、无网络请求

## 2. 功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| F1 | seed 输入 | 页面提供输入框，用户输入任意字符串 |
| F2 | 确定性生成 | 同一 seed → SHA-256 哈希一致 → PRNG 序列一致 → 头像完全一致 |
| F3 | URL 参数支持 | `index.html?seed=xxx` 打开即生成对应头像，方便分享 |
| F4 | URL 同步 | 输入变化时同步更新地址栏（history.replaceState），刷新不丢 |
| F5 | 随机 seed | 一键生成随机 seed（crypto.getRandomValues） |
| F6 | 下载 SVG | 一键下载当前头像为 `.svg` 文件 |
| F7 | 复制链接 | 一键复制带 seed 的页面链接 |
| F8 | 部件化组装 | 头像由多层部件分层拼合：**背景→尾巴（身后）→身体填充→身体花纹→大件服装→身体描边→脖子件→耳朵→兜帽猫耳→帽壳→脸填充→头部花纹→脸描边→五官→配饰→左右物品**。关键遮挡关系：身体描边（m-body-line）压在花纹/大件服装上出轮廓、画在脖子件/兜帽**之下**（围巾垂布/围兜/帽沿盖住身体边缘黑线，不被黑线横切）；**兜帽拆两层**——兜帽猫耳画在真耳**之上**（帽耳同位同形盖住真耳，耳朵穿进帽耳里）、帽壳画在帽耳**之上**（帽壳椭圆盖住耳根，帽耳尖端从帽壳顶伸出）、帽壳在脸填充之前（脸盖住帽壳中间只露一圈帽沿）；尾巴渲染在身体下层（从身后绕出）。清单见 §5 |
| F9 | 稀有品种猫 | **1% 概率**命中品种猫（命中后各品种**等概率**）；命中时大部分五官由品种整体设计固定，仅少数部件继续随机，规则见 §6 |

## 3. 非功能需求

- **纯前端**：双击 `index.html` 或任意静态服务器均可运行
- **无第三方依赖**：不引入任何 npm 包 / CDN 脚本
- **安全上下文**：Web Crypto API 的 `crypto.subtle` 需要安全上下文；`file://`、`localhost`、HTTPS 均满足
- **可访问性**：输入框有 label、按钮有 aria-label、SVG 带 `role="img"` 与 `aria-label`

## 4. 技术方案

### 4.1 核心流程

```
seed 字符串
   │  TextEncoder → Uint8Array
   ▼
crypto.subtle.digest('SHA-256')          （异步，返回 32 字节哈希）
   │  哈希字节 → xmur3 种子混合 → mulberry32
   ▼
确定性 PRNG（每次调用产生相同的伪随机序列）
   │  依次为每个部件抽取：样式索引、颜色、位置参数
   ▼
拼装 SVG 字符串（模板函数按部件拼接 <path>/<circle>/<g>）
   ▼
innerHTML / DOM 渲染到展示区
```

### 4.2 关键实现点

1. **哈希即种子**：不做「哈希后取模」这种一次性映射，而是把 32 字节哈希喂给 PRNG，按需消费随机数——部件增减不影响已消费序列的稳定性，扩展性好。
2. **PRNG 选型**：`mulberry32`（~5 行、分布足够、确定性可靠），用 `xmur3` 字符串哈希做种子混合可直接吃掉哈希字节。
3. **颜色生成**：HSL 空间随机色相 + 限定饱和度/亮度区间，保证好看；脸部/背景用对比色区间避免糊成一团。
   - **统一毛色**：头部、身体、尾巴共用同一个毛色（抽取一次，三处复用）。
   - **花纹色**：在统一毛色基础上明度 ±5%~10%（淡或深，随机决定），同 seed 结果固定。
4. **部件定义**：每个部件 = `{ variants: [SVG 生成函数], count }`，新增部件只改数据不动逻辑。
5. **随机数消费顺序固定**：PRNG 按约定顺序消费（**品种判定〔普通猫 / 品种猫及具体品种〕**→背景色→背景图案→统一毛色→尾巴动作→左耳→右耳→耳内毛→眼〔样式→是否异瞳→虹膜色；闭眼则跳过颜色〕→嘴→胡须〔颜色档+根数〕→各可选件〔眉毛、花纹：头部花纹/额外花纹/尾巴花纹、腮红、服装、配饰、左右物品〕是否出现→已出现可选件的样式与参数→**T恤/卫衣图案与底色**〔第 21-22 位，仅服装为小 T 恤/连帽卫衣时消费〕）。将来新增部件的随机数必须**追加在序列末尾**，否则会改变旧 seed 的头像。品种猫命中时走 §6 的裁剪序列。

### 4.3 文件结构

```
random-cat-avatar/
├── index.html       # 展示页：头像展示区、seed 输入框、按钮组（骨架，无逻辑）
├── style.css        # 布局与样式
├── avatar.js        # 纯生成模块：输入 seed → 输出 SVG 字符串，零 UI、零 DOM 依赖，可被任何页面 import
├── parts.js         # 部件数据：20 类部件（大部分每类 ≥10 个候选，头部花纹 5 个、胡须 2 色）
├── breeds.js        # 品种猫整体设计（1% 彩蛋，12 种常见品种等概率，各含定妆图纸）
├── ui.js             # UI 交互：读输入/URL → 调 avatar.js → 渲染、下载、复制链接（**不能叫 main.js**——Vercel 零配置会把根目录 main.js/server.js 当整个部署的 Node 入口导致 500）
├── server.js        # 零依赖 Node 服务器：静态托管 + /avatar.svg?seed=xxx 动态端点（vm 复用 parts/breeds/avatar 三文件，逻辑与浏览器端 100% 一致）→ `<img>` 标签可直接引用
└── REQUIREMENTS.md  # 本文档
```

> **`<img>` 引用**：`node serve.js`（默认端口 8123，`PORT` 环境变量可改）后，`<img src="/avatar.svg?seed=任意内容">` 直接出图；同 seed 同图可缓存（Cache-Control 24h）、`Access-Control-Allow-Origin: *` 允许跨站引用、seed 经 SHA-256 无注入面、目录穿越已防护。Vercel 部署同端点。
> **测试站部署（宝塔 nginx）**：站点 root=D:/repos/random-cat-avatar 纯静态，`/avatar.svg` 由反代提供——代理配置在 `D:/BtSoft/nginx/conf/proxy/randomcatavatar.test/avatar.conf`（location = /avatar.svg → 127.0.0.1:8123；8000 被本机 Python 占用，故 node 用 `PORT=8123` 启动）。改代理配置后需重载 nginx（服务为 SYSTEM 权限，普通终端 reload 会 Access denied，需宝塔面板或管理员终端执行）。

> **模块接口约定**：`avatar.js` 对外只暴露一个函数 `renderAvatar(seed) → Promise<string>`（返回完整 SVG 字符串，内部完成 SHA-256 → PRNG → 部件抽取 → 拼装全流程）。它不碰 DOM、不碰 location，理论上任何项目 `import` 走就能用，也可以直接拿返回值当静态资源。
>
> 部件候选总量 100+，数据量足以单开 `parts.js`；逻辑与数据分离后，加样式不用碰生成逻辑。

## 5. 部件清单（已确认）

整体：以猫为主体，**正面构图**（无侧面），头 + 身体 + 尾巴，卡通线条风格。分**必有**与**可选**两类：

### 5.1 必有部件（每个 seed 都出现，每类 ≥10 个候选样式）

| 层级 | 部件 | 说明 |
|------|------|------|
| 1 | 背景色 | 纯色，随机取色（≥10 组配色） |
| 2 | 背景图案 | 圆点/条纹/波纹等，叠在背景色上（≥10 种，含「无图案」档位） |
| 3 | 尾巴 | 尾巴动作/形态变化，**渲染在身体下层**（从身后绕出），颜色 = 统一毛色（≥10 种） |
| 4 | 身体 | 猫的身体（正面姿态/体型变化），颜色 = 统一毛色 |
| 5 | 脸与底色 | 正面脸部形状，颜色 = 统一毛色 |
| 6 | 左耳 | 独立部件：外耳形状 + 毛色独立随机，可与右耳不同（≥10 种） |
| 7 | 右耳 | 独立部件：外耳形状 + 毛色独立随机，可与左耳不同（≥10 种） |
| 8 | 耳内毛 | 独立部件：内耳样式 + 颜色**左右两耳共用同一份**（外观与颜色完全一致，≥10 种） |
| 9 | 眼睛 | 样式与颜色；**30% 概率异瞳**（左右眼虹膜各取一色，固定可复现），普通情况双眼同色；**闭眼样式不渲染虹膜颜色**（仅线条） |
| 10 | 嘴巴 | 样式（决定表情） |
| 11 | 胡须 | 脸颊两侧胡须，**每个 seed 必定出现**：**仅纯白或纯黑两档**（一次抽取、全程同色，不混色），每边 **3~5 根**（根数由 seed 决定、**左右数量对称**、镜像布局、固定可复现），描边式细线 |

### 5.2 可选部件（是否出现由 seed 决定，出现与否均固定可复现）

| 部件 | 说明 | 出现概率（初值，可调） |
|------|------|------|
| 头部花纹 | **仅叠在头部**的花纹（额头/头顶区域，**5 种外观**），颜色 = 统一毛色明度 ±5%~10%（比头部浅或深，随机且固定可复现），或完全不显示 | ~50% |
| 额外花纹 | **仅叠在身体上**的花纹（虎斑/奶牛斑/条纹等，≥10 种；m-body-edge 裁剪 = 身体挖掉脸，绝不爬上脸；画在脖子件之下，不盖围兜/项圈/领结/围巾），颜色 = 统一毛色明度 ±5%~10%（比身体淡或深，随机且固定可复现） | ~50% |
| 尾巴花纹 | **仅叠在尾巴上**的花纹（环纹/尖端色块等，≥10 种），其余配置与额外花纹一致：颜色 = 统一毛色明度 ±5%~10%，独立判定是否出现 | ~50% |
| 腮红 | 脸颊腮红 | ~50% |
| 眉毛 | 眼睛上方的眉毛，**≥10 种外观**（形状/角度/粗细变化，决定神态：严肃/无辜/惊讶/生气…），左右一对成组出现，**颜色仅纯黑或灰色两档**（一次抽取、全程同色） | ~70% |
| 服装 | 身体上的衣服/围巾/领结等 | ~50% |
| 配饰 | 帽子/眼镜/蝴蝶结等（戴在头上） | ~40% |
| 左侧物品 | 画面左侧物品（小鱼/毛线球/奶瓶等） | ~35% |
| 右侧物品 | 画面右侧物品 | ~35% |
| 心情符号 | **头像左上角**的心情标记（最上层，中心约 (96,96)），9 种等概率：粉色爱心、黄色四角星（四边向内弯的弧+左下角一颗缩小的小四角星）、音符、生气怒符（井字四边全向中心凹的弧线；弧端切线各倾 50°，相邻弧夹角 80°；四边间隙 42~53px、角落端点分开；笔画 13）、问号（钩子沿左侧上顶绕右侧下弯回中心，下接一个点）、双问号（两个 0.75 缩小的问号并排）、感叹号、双感叹号、三个点；**问号/感叹号类带 12° 向左倾斜**（rotate(-12)）；随机数固定消费在第 23 位（追加末尾，不动老 seed 的 1-22 位） | ~60% |

可选部件同样每类 ≥10 个候选样式。左右物品、服装、配饰、腮红的「出现与否」由 PRNG 独立随机位决定——同一个 seed 刷新一万次结果也一致。

### 5.3 开发前约定

> **部件外观需逐类确认**：本文档只锁定了部件类别与数量下限。正式编写 `parts.js` 的部件外观时，须先与用户确认每一类部件的具体样式清单（长什么样、要哪些），确认后再动手。

## 6. 稀有品种猫（彩蛋机制）

- **触发**：每次生成**最先**判定品种——**99.9%** 普通（完全随机拼装），**0.1%** 命中品种猫；命中后在品种池内**等概率（平均分配）**抽取具体品种。
- **品种清单（12 种常见款，各 1/12）**：英短蓝猫、美短（经典银虎斑）、狸花猫、橘猫、奶牛猫（黑白）、三花猫、暹罗猫、布偶猫、加菲猫（异国短毛）、波斯猫、缅因猫、俄罗斯蓝猫。每个品种一张「定妆图纸」固定毛色/耳/眼/花纹等特征（如：暹罗=重点色+蓝眼+深色脸罩（头部花纹 #6，脸中央面具）、狸花=棕色虎斑纹、三花=白底橘黑斑块）。
- **命中品种猫时的部件规则**：品种猫是一套**整体定妆设计**——统一毛色、尾巴动作、左右耳、耳内毛、眼睛、胡须、头部/额外/尾巴花纹、腮红全部由品种图纸固定，不参与随机。此时仍继续随机生成的部件**仅有 8 个**：

| 是否可选 | 生效部件 | 说明 |
|------|------|------|
| 必有 | 背景色 | 照常随机 |
| 必有 | 背景图案 | 照常随机（含「无图案」档） |
| 必有 | 嘴 | 照常随机 |
| 可选 | 眉毛 | ~70% 出现，照常黑/灰二选一 |
| 可选 | 服装 | ~50% 出现 |
| 可选 | 配饰 | ~40% 出现 |
| 可选 | 左侧物品 | ~35% 出现 |
| 可选 | 右侧物品 | ~35% 出现 |

- 可选部件**照常按原概率独立判定**是否出现（「该是概率出现的还是概率出现」）。
- 一切判定仍由 seed 驱动，同一 seed 刷新结果固定不变。

## 7. 部件外观清单（已确认）

> **构图基准（线稿定稿）**：正面大头圆脸、耳朵长在头顶、圆眼、胡须横向伸出脸颊、身体为简洁梯形——**不画四肢/脚/爪**，构图只有头部 + 身体 + 尾巴。所有部件坐标基于此构图。

> **线宽规范（统一）**：主体轮廓与线状五官（脸/身/耳/尾边缘/嘴/闭眼线/服装/配饰/物品主轮廓）统一 `stroke-width: 6`；睁眼类填充眼描边 `5`；胡须 `4.5`（毛发）；内部小细节（铃铛/十字/羽毛等）`3~4`；眉毛按样式粗细（细 3.5 / 标准 6 / 短粗 9）。**身体描边用 m-body-line mask**（填充+12 描边带全白+脸黑挖掉下巴遮挡区）：若直接用 m-body-edge（仅填充白）会吃掉描边外半圈只剩 3px，黑边比脸细一半。

### 7.1 必有部件

| 部件 | 样式清单 |
|------|----------|
| 尾巴 ×10 | 问号卷尾、S 型上扬、自然下垂、绕成一圈、笔直竖起、波浪摇尾、夹腿害羞、卷成球、微微上翘、蓬蓬炸毛尾 |
| 身体 ×10（梯形系，无脚） | 标准梯形、圆胖梨形、瘦长条、矮胖墩、毛茸蓬蓬、蹲坐馒头、宽扁、直立长方、超肥、幼猫迷你 |
| 脸型 ×10 | 标准圆脸、胖圆脸、高圆脸、方脸、小圆脸、包子脸、扁脸、竖椭圆脸、大圆脸、宽圆脸。**全部圆底无下巴**（不做尖下巴/心形等人形下巴） |
| 左耳/右耳（形状固定为**标准三角形**，各自独立随机毛色） | 标准三角 ×1 |
| 耳内毛 ×10（左右共用同款同色） | 素色内耳、内耳+耳尖变色、双色内耳、条纹内耳、白边内耳、粉系内耳、深色系内耳、渐变内耳、无色差内耳、点状内耳 |
| 眼睛 ×14 | 标准圆眼、竖椭圆眼（猫瞳）、眯眯眼（^ ^）、闭眼弧线（不渲染虹膜色）、星星眼、爱心眼、>< 挤眼、闪亮大圆眼、下弯月牙、小圆点眼、上半圆眼、下半圆眼、委屈眼（下半圆 75% 外垂 30°）、愤怒眼（下半圆 75% 内压 30°） |
| 嘴巴 ×6（中心 (256,276)） | w 形、u 形、v 形、倒 v 形、圆形（实心）、倒三角形（实心） |
| 胡须 | 纯白 / 纯黑 × 每边 3~5 根（扇形排布，仅颜色与根数随机，左右对称） |

### 7.2 可选部件

| 部件 | 样式清单 |
|------|----------|
| 头部花纹 ×5 | 额头 M 纹、头顶斑、额头竖纹、耳边色块、额头心形斑 |
| 额外花纹 ×10 | 虎斑纹、奶牛黑白斑、燕尾服（白胸白手）、双色斑块、全身条纹、重点色、渐层、玳瑁斑、圆点斑、螺旋纹 |
| 尾巴花纹 ×10 | 节节环纹、尾尖色块、条纹尾、双色尾、渐变尾、斑点尾、螺旋尾、双色环、尾尖白、满色尾 |
| 腮红 ×10 | 粉圆、橘粉椭圆、红斜线三道、渐变圆、心形、斜椭圆、双圆点、大圆、小圆、三角形（颜色随样式自带） |
| 眉毛 ×10（纯黑/灰） | 平直眉、上挑眉、八字无辜眉、倒八字生气眉、拱形眉、斜上眉、斜下眉、波浪眉、短粗眉、细长眉 |
| 服装 ×12 | 领结、围巾、小 T 恤、**连帽卫衣**、**衬衫**、背带裤、毛衣、围兜、小披风、护士服、小背心、项圈铃铛。**大件轮廓 = 身体路径本身**（fn(d) 直接拿身体路径填充，身体多大衣服多大，零缩放零形变，不再被撑大/压扁）；细节（领口线/条纹/纽扣等）画超宽（x120~392）由 m-body-edge 裁进身体且绝不盖脸，黑边复用身体描边。**T恤底色 ×6**（天蓝/奶黄/草绿/樱粉/香芋紫/活力橘，亮色，每色配同系深色领口线）、**图案 ×8**（小鱼/爪印/爱心/星星/海魂条纹/波点/骨头/闪电）：**100% 有图案**（T恤和卫衣都画同一套图案）、8 种等概率，随机数固定消费在第 21-22 位（仅 T恤/卫衣时消费，老 seed 的 1-20 位不变）。**连帽卫衣**：衣身 6 色（藏蓝/玫红/松绿/深紫/赭橙/深青，比 T恤同系深一档）；**兜帽 = 脸椭圆 +18px 的一圈**（帽壳椭圆按脸型 cy/rx/ry 定位，真正围住头部，脸盖住中间只露一圈帽沿）、兜帽更深一档且**猫耳与真耳同位同形**（盖住真耳 = 耳朵"穿进"帽里）+ 耳内阴影；兜帽单独画在耳朵之后脸之前（不被脸裁剪、不挡五官），复用第 21-22 位随机数取色。**衬衫**：复用 T恤 6 底色，翻领两片；**扣上/敞开各 50%**（第 24 位，仅衬衫时消费）——扣上 = 门襟线 + 5 颗白扣，敞开 = **竖直开口条露毛色**（x236-276 平行竖线到衣底，无内搭、不斜、服装互斥单选）+ 左襟两颗未扣的扣子；**50% 概率花纹**（复用第 21 位：0-7 有图案 / 8-15 素面），花纹为 **SHIRT_PATS 单色系**（同系深一档色 lc）：大方格、细密小方格、重复小三角、重复小菱形、交错圆点、重复小十字、斜条纹、横条纹——方格/重复几何图形，**无 T恤那套彩色图案**；敞开时花纹被竖直开口条切掉中间、两侧衣襟各留一半。脖子小件（领结/围巾/围兜/项圈）**画在脸之前**（围兜/项圈顶部伸入脸的区域被脸盖住，不挡头）、**随脸底平移贴合下巴且不缩放**（脖子宽度与身体无关，任何身体上同尺寸），围巾垂布**按当前身体实际高度取长度**（垂布底 y=faceBottom+151 随脸底/身体联动，矮身体变短高身体变长）、项圈弧顶 y337 上移更贴下巴、围兜（口水巾）顶伸入脸后由脸盖住 |
| 配饰 ×10 | 蝴蝶结、贝雷帽、小草帽、圆框眼镜、方框眼镜、皇冠、耳机、发夹、花环、迷你皇冠。**帽子类（贝雷帽/草帽/皇冠/耳机/花环/迷你皇冠）跟随脸型头顶自动平移**（10 种脸头顶 y=88~119，按 faceTop-97 偏移），不再悬浮；眼镜固定于眼位、蝴蝶结/发夹夹在耳朵上 |
| 左/右物品 ×10（独立判定） | 小鱼干、毛线球、奶瓶、鱼骨头、猫抓板、罐头、逗猫棒、毛球、老鼠玩具、纸箱 |

### 7.3 颜色池

- **背景色 ×10 组**（底色+图案色成对）：奶油白、薄荷绿、天蓝、樱粉、鹅黄、薰衣草紫、珊瑚橙、湖水绿、灰蓝、暖米灰
- **毛色池 14**：白、奶黄、橘、浅橘、灰、蓝灰、烟灰、黑、奶油、棕、米、粉灰、**银灰、深灰**（头部/身体/尾巴共用其一；左右耳 **80% 双耳同身体色 / 15% 单耳异色（侧随机）/ 5% 双耳独立抽取**）
- **毛色微调机制**：耳/头/身/尾的每个**不同**毛色独立随机 **±0~2.5% 明度**（淡或深，方向与幅度随机）——同色部件用同一结果（头身尾永远同色、双耳同色时也一致）；由 **SHA-256 第 2 个 4 字节的副 PRNG** 生成，**不消耗主随机序列**；品种猫定妆毛色同样参与微调
- **虹膜色 ×10**：蓝、绿、金、琥珀、黄、湖水、紫、棕、浅蓝、橙红（异瞳时左右各一色）
- **花纹色**：毛色明度 ±5%~10%（浅/深随机，三种花纹各自独立抽取方向）
